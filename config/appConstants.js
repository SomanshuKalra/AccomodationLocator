/**
 * Author: Somanshu Kalra
 * Description: File to hold messages, texts and response codes & messages
 * Date: 24 April 2020
 *
 */

const CONTENT_MESSAGES = {
  GENERIC_MESSAGES: {
    API_RUNNING: 'API running',
    DB_CONNECTED: 'Connected to database!',
  },
};

const APP_CONSTANTS = {
  CONTENT_MESSAGES: CONTENT_MESSAGES,
};

module.exports = APP_CONSTANTS;
